<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Pdf.Views.ConcatePdfDocs">
    <!--<NavigationPage.TitleView>
        <StackLayout Orientation="Horizontal">
            <Label Text="Pdf Edit"/>
        </StackLayout>
    </NavigationPage.TitleView>-->

    <StackLayout  x:Name="stkl">
        <CollectionView x:Name="FilesList" SelectionMode="Multiple">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Orientation="Horizontal">
                        <StackLayout Orientation="Vertical">
                            <Label Text="{Binding Name}"/>
                            <Label Text="{Binding CreationTime.Date, StringFormat='{}{0:dd/MM/yyyy}'}"/>
                        </StackLayout>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Button Text="Go" Clicked="StartTheConvertion"/>
    </StackLayout>
</ContentPage>